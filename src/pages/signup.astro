---
import Layout from "../layouts/Layout.astro";
import { AuthFormSignup } from "../components/auth/AuthFormSignup";

// Note: When backend auth is implemented, this page should redirect
// authenticated users to "/" to avoid showing the signup form unnecessarily
---

<Layout title="Sign up — Cook Mastery">
  <div class="container mx-auto px-4 py-8 md:py-16">
    <AuthFormSignup client:load />
  </div>
</Layout>
